from PyAPI.endpoints import article_ideas as AI
import requests

if __name__ == "__main__":
    """ 
    This code does not interfere with the PyAPI package. 
    And is only meant to be used for command line testing.  
    """
pass
